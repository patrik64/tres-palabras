# Supabase Postgres connection string (Transaction pooler, port 6543).
# Supabase dashboard -> Connect -> Connection string -> Transaction pooler.
# When unset, the app falls back to local JSON files under ./db.
DATABASE_URL=postgresql://postgres.<project-ref>:<db-password>@<region>.pooler.supabase.com:6543/postgres
